source 'https://github.com/CocoaPods/Specs.git'
use_frameworks!

target 'ActionCableClient_Example', :exclusive => true do
  pod 'ActionCableClient', :path => '../'
end

target 'ActionCableClient_Tests', :exclusive => true do
  pod 'ActionCableClient', :path => '../'

  pod 'Quick', '~> 0.8.0'
  pod 'Nimble', '3.0.0'
end
